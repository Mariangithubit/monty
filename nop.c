#include "monty.h"

/**
 * f_nop - doesn’t do anything.
 * @head: the head of stack 
 * @counter: line_number
 * Return: void
 */
void f_push(stack_t **head, unsigned int counter)
{
	(void)counter;
	(void)head;
}
